# Generative Adversarial Nets

> Ian J. Goodfellow et al., NeurIPS 2014, arXiv:1406.2661  
> 한 줄 요약: 생성자와 판별자를 경쟁시키는 minimax game으로 명시적 likelihood나 Markov chain 없이 데이터 분포를 학습하는 새로운 생성 모델 프레임워크를 제안한다.

## 0. 핵심 요약

GAN(Generative Adversarial Network)은 두 신경망을 동시에 학습한다.

- **Generator `G`**: noise `z`를 데이터 공간의 가짜 샘플 `G(z)`로 변환한다.
- **Discriminator `D`**: 입력이 실제 데이터에서 왔을 확률 `D(x)`를 출력한다.

판별자는 실제와 가짜를 잘 구별하려 하고, 생성자는 판별자를 속이려 한다. 이 경쟁은 다음 minimax game으로 표현된다.

$$
\min_G\max_D V(D,G)
=\mathbb{E}_{x\sim p_{\text{data}}}[\log D(x)]
+\mathbb{E}_{z\sim p_z}[\log(1-D(G(z)))]
$$

함수의 표현력이 무한하고 매 단계에서 판별자가 최적화된다는 이상적 조건에서는 최적점이 `p_g=p_data`이고 `D(x)=1/2`임을 증명한다. 실전에서는 판별자와 생성자를 번갈아 gradient로 학습한다. 초기 생성자가 나쁠 때 minimax generator loss가 포화되므로, 같은 고정점을 갖지만 gradient가 더 강한 non-saturating loss `-log D(G(z))`를 사용한다.

## 1. 어떤 문제를 해결하려 했는가? (Abstract)

2014년 당시 심층 생성 모델은 다음 계산 문제를 안고 있었다.

- Maximum likelihood 학습에 필요한 확률 계산이나 정규화 상수가 다루기 어렵다.
- 잠재변수 posterior를 정확히 계산하기 어려워 approximate inference가 필요하다.
- RBM/DBM 같은 모델은 MCMC와 partition function 근사가 필요하다.
- Markov chain 기반 생성은 mixing 문제를 겪고 sample 생성도 반복적이다.
- 생성 모델에서 backpropagation과 piecewise-linear unit의 장점을 온전히 활용하기 어려웠다.

논문은 likelihood를 명시적으로 계산하지 않고도, 판별 문제의 gradient를 이용해 생성 모델을 학습하는 adversarial framework를 제시한다. MLP로 구현하면 backpropagation만으로 학습하고 한 번의 forward pass로 샘플을 만들 수 있다.

## 2. 연구 동기와 문제점 (Introduction)

당시 deep learning의 큰 성공은 입력을 label로 매핑하는 discriminative model에 집중돼 있었다. 반면 이미지·음성·언어의 복잡한 분포 자체를 학습하는 deep generative model은 확률적 계산과 sampling이 어려워 발전이 더뎠다.

저자들은 위조범과 경찰의 비유를 사용한다.

- 생성자는 진짜처럼 보이는 위조지폐를 만든다.
- 판별자는 진짜와 위조를 구분한다.
- 서로 경쟁하며 개선된 끝에 위조 샘플과 실제 샘플을 구분할 수 없는 균형에 도달한다.

핵심 발상은 생성 분포의 likelihood를 직접 계산하는 대신, **학습 가능한 판별자의 피드백을 생성자 학습 신호로 사용**하는 것이다.

## 3. 관련 연구 동향 (Related Works)

### 3.1 Directed graphical model

잠재변수가 있는 directed model은 posterior inference가 어려워 variational inference나 별도의 inference network가 필요할 수 있다. GAN은 생성 시 `z -> G(z)`의 직접 mapping을 사용하고 학습 중 posterior inference를 요구하지 않는다.

### 3.2 Undirected graphical model

RBM과 DBM은 전역 partition function과 그 gradient 계산이 어렵다. MCMC로 근사할 수 있지만 chain mixing이 병목이다. GAN은 학습과 sampling 모두 Markov chain을 요구하지 않는다.

### 3.3 Score matching과 NCE

Score matching과 Noise-Contrastive Estimation도 likelihood 이외의 기준으로 분포를 학습한다. 그러나 density가 정규화 상수까지는 아니더라도 해석적으로 표현돼야 한다. NCE는 고정된 noise distribution과 데이터를 구분하지만, GAN의 noise 경쟁자는 고정되어 있지 않고 생성자가 계속 개선된다.

### 3.4 Generative stochastic networks와 autoencoder

Denoising autoencoder와 GSN은 생성 Markov chain의 transition을 학습한다. GAN은 feedback loop 없이 한 번의 generator forward pass로 서로 독립적인 샘플을 생성한다.

### 3.5 동시기 VAE 계열

논문은 Auto-Encoding Variational Bayes와 stochastic backpropagation도 언급한다. VAE가 likelihood lower bound와 approximate posterior를 이용하는 명시적 잠재변수 모델이라면, GAN은 discriminator가 정의한 학습 신호로 implicit distribution을 학습한다.

## 4. 연구 접근법과 모델 구조 (Method)

### 4.1 Generator

noise prior에서 잠재벡터를 sampling한다.

$$
z\sim p_z(z)
$$

미분 가능한 MLP `G(z;θ_g)`가 이를 데이터 공간으로 보낸다.

$$
x_{\text{fake}}=G(z;\theta_g)
$$

이 mapping이 유도하는 implicit distribution을 `p_g`라 한다. `p_g(x)`의 density를 직접 계산할 필요는 없다.

### 4.2 Discriminator

`D(x;θ_d)`는 scalar를 출력하며, 이를 `x`가 `p_data`에서 왔을 확률로 해석한다.

$$
D(x)\approx P(y=1\mid x)
$$

실제 샘플에는 1, 생성 샘플에는 0을 할당하도록 binary cross-entropy를 최대화한다.

### 4.3 Minimax objective

$$
\min_G\max_D
\left[
\mathbb{E}_{x\sim p_{\text{data}}}\log D(x)
+\mathbb{E}_{z\sim p_z}\log(1-D(G(z)))
\right]
$$

- `D` 단계: `log D(x) + log(1-D(G(z)))`를 최대화한다.
- `G` 단계: 원래 minimax 식에서는 `log(1-D(G(z)))`를 최소화한다.

논문 실험은 판별자 `k`회, 생성자 1회를 번갈아 업데이트하며 `k=1`을 사용했다. 매 iteration마다 판별자를 완전히 최적화하면 계산량이 크고 유한 데이터에 overfit할 수 있기 때문이다.

### 4.4 Non-saturating generator loss

학습 초기에 판별자가 생성 샘플을 쉽게 거부하면 `D(G(z))≈0`이고 원래 generator objective의 gradient가 약해진다. 논문은 다음 목적을 대신 최대화한다.

$$
\max_G\mathbb{E}_{z\sim p_z}[\log D(G(z))]
$$

구현에서는 보통 다음을 최소화한다.

$$
L_G=-\mathbb{E}_{z\sim p_z}\log D(G(z))
$$

원래 minimax 목적과 같은 이상적 fixed point를 갖지만 초기 gradient가 더 강하다.

## 5. 이론적 분석

### 5.1 고정된 생성자에 대한 최적 판별자

`G`가 고정돼 있을 때 판별자의 목적은 각 `x`에서 다음을 최대화하는 것이다.

$$
p_{\text{data}}(x)\log D(x)+p_g(x)\log(1-D(x))
$$

이를 `D(x)`로 미분해 0으로 두면 다음 최적 판별자를 얻는다.

$$
D_G^*(x)=\frac{p_{\text{data}}(x)}{p_{\text{data}}(x)+p_g(x)}
$$

실제 분포의 density가 더 큰 곳에서는 1에, 생성 분포의 density가 더 큰 곳에서는 0에 가까워진다.

### 5.2 Generator가 실제로 최소화하는 것

최적 판별자를 value function에 대입한다.

$$
C(G)=V(D_G^*,G)
$$

정리하면 다음과 같다.

$$
C(G)=-\log 4+2\,\mathrm{JSD}(p_{\text{data}}\Vert p_g)
$$

Jensen-Shannon divergence는 항상 0 이상이며 두 분포가 같을 때만 0이다. 따라서 전역 최솟값은 다음과 같다.

$$
p_g=p_{\text{data}},\qquad C(G)=-\log4
$$

이때 최적 판별자는 어느 입력에서도 출처를 구분할 수 없어 `D(x)=1/2`가 된다.

### 5.3 증명의 적용 범위

이 보장은 다음 이상적 조건에 한정된다.

- `G`와 `D`가 충분한 표현력을 가진다.
- 각 generator step에서 판별자가 최적점에 도달한다.
- 확률분포 공간에서 충분히 작은 update를 한다.

실제 신경망은 유한한 capacity와 비볼록 parameter space를 가지므로, 이 정리가 실제 SGD가 항상 전역 균형에 도달한다는 보장은 아니다.

## 6. 학습 알고리즘과 코드 분석

### 6.1 논문의 Algorithm 1

1. 실제 minibatch `x`와 noise minibatch `z`를 뽑는다.
2. `D(x)`는 높이고 `D(G(z))`는 낮추도록 discriminator를 업데이트한다.
3. 새 noise `z`를 뽑는다.
4. generator가 discriminator를 속이도록 업데이트한다.

### 6.2 PyTorch 개념 구현

아래 코드는 논문의 알고리즘을 현대적인 non-saturating loss로 옮긴 최소 예다. 논문 공식 코드의 전재가 아니라 수식의 동작을 설명하기 위한 코드다.

```python
import torch
from torch import nn

bce = nn.BCEWithLogitsLoss()


def discriminator_step(generator, discriminator, real, opt_d, latent_dim):
    batch = real.size(0)
    z = torch.randn(batch, latent_dim, device=real.device)

    # D를 학습할 때 G까지 gradient가 흐르지 않게 한다.
    with torch.no_grad():
        fake = generator(z)

    real_logits = discriminator(real)
    fake_logits = discriminator(fake)
    loss_d = bce(real_logits, torch.ones_like(real_logits))
    loss_d += bce(fake_logits, torch.zeros_like(fake_logits))

    opt_d.zero_grad()
    loss_d.backward()
    opt_d.step()
    return loss_d.item()


def generator_step(generator, discriminator, batch, opt_g, latent_dim, device):
    z = torch.randn(batch, latent_dim, device=device)
    fake = generator(z)
    fake_logits = discriminator(fake)

    # -log D(G(z)): 생성 샘플을 real(1)로 판정하게 만든다.
    loss_g = bce(fake_logits, torch.ones_like(fake_logits))

    opt_g.zero_grad()
    loss_g.backward()
    opt_g.step()
    return loss_g.item()
```

중요한 코드 포인트는 다음과 같다.

- `BCEWithLogitsLoss`를 쓰면 sigmoid와 log 계산을 합쳐 수치적으로 안정적이다.
- discriminator step의 fake는 `detach()` 또는 `no_grad()` 처리해야 불필요한 generator gradient를 만들지 않는다.
- generator step에서는 discriminator parameter를 업데이트하지 않더라도 **discriminator를 통과하는 gradient는 유지**해야 한다.
- `loss_g`의 target이 1인 것이 non-saturating objective `-log D(G(z))`에 해당한다.
- `D` accuracy가 높다고 GAN 전체가 잘 학습되는 것은 아니다. 두 모델의 균형이 중요하다.

## 7. 실험 구성 (Experiments)

### 7.1 데이터셋과 모델

- MNIST
- Toronto Face Database(TFD)
- CIFAR-10
- Generator: rectifier linear activation과 sigmoid의 조합
- Discriminator: maxout activation과 dropout
- Generator의 noise는 가장 아래 입력층에만 주입
- 최적화: momentum 기반 gradient method

논문은 fully-connected CIFAR-10 모델과 convolutional discriminator/deconvolutional generator의 sample을 모두 제시한다.

### 7.2 평가 방법

GAN은 explicit likelihood를 제공하지 않으므로 생성 sample에 Gaussian Parzen window를 적합하고 test data의 log-likelihood를 근사했다. Gaussian bandwidth `σ`는 validation set으로 선택했다.

저자들은 이 평가가 high variance이고 고차원에서 잘 작동하지 않는다는 한계를 직접 인정한다. 따라서 수치 비교와 함께 생성 sample, latent interpolation, nearest training example을 제시했다.

### 7.3 정량 결과

| 모델 | MNIST log-likelihood | TFD log-likelihood |
|---|---:|---:|
| DBN | 138 ± 2 | 1909 ± 66 |
| Stacked CAE | 121 ± 1.6 | **2110 ± 50** |
| Deep GSN | 214 ± 1.1 | 1890 ± 29 |
| **Adversarial nets** | **225 ± 2** | 2057 ± 26 |

GAN은 MNIST에서 표의 비교군 중 가장 높은 Parzen estimate를 기록했고 TFD에서도 경쟁력 있는 결과를 보였다. 다만 평가기의 한계가 크므로 절대적 likelihood 우위로 해석하면 안 된다.

### 7.4 정성 결과

- MNIST, TFD, CIFAR-10에서 생성 sample을 제시했다.
- 생성 sample의 nearest training example을 함께 보여 단순 암기가 아님을 점검했다.
- 두 latent vector 사이를 선형 보간했을 때 digit가 부드럽게 변했다. 이는 `G`가 latent space에서 의미 있는 연속 구조를 학습했음을 시사한다.
- 논문은 sample이 기존 방법보다 우수하다고 단정하지 않고, adversarial framework의 가능성을 보여주는 수준으로 표현한다.

## 8. 무엇을 발견했고 한계점은 무엇인가? (Discussion)

### 8.1 발견과 장점

1. Generator density를 명시적으로 쓰지 않고도 sample 기반 학습이 가능하다.
2. 학습에는 backpropagation만 필요하며 approximate inference와 MCMC가 필요 없다.
3. 생성 시 `G(z)` 한 번으로 sample을 얻으므로 Markov chain mixing 문제가 없다.
4. Generator와 discriminator에 미분 가능한 다양한 신경망을 사용할 수 있다.
5. Generator가 training example을 직접 복사하는 loss가 아니라 discriminator의 gradient만 받는다는 통계적 특성이 있다.
6. Markov chain이 이동할 수 있도록 분포를 부드럽게 만들 필요가 없어 sharp하거나 퇴화한 분포도 표현할 수 있다.

### 8.2 논문이 밝힌 한계

- `p_g(x)`의 explicit representation이 없어 likelihood를 직접 계산하기 어렵다.
- `G`와 `D`를 적절히 동기화해야 한다.
- `G`를 `D`보다 지나치게 많이 학습하면 여러 `z`를 같은 `x`에 매핑해 다양성이 사라질 수 있다. 논문은 이를 “Helvetica scenario”로 표현하며 오늘날 mode collapse로 알려진 문제와 연결된다.
- 원래 minimax generator objective는 학습 초기에 gradient가 포화될 수 있다.
- 실험 규모와 평가 지표가 제한적이며 고차원 sample quality를 신뢰성 있게 수치화하기 어려웠다.

### 8.3 비판적으로 볼 점

- 이론은 distribution space와 optimal discriminator를 가정하지만 실제 alternating SGD는 이 조건을 만족하지 않는다.
- 두 플레이어의 objective가 동시에 변하므로 일반적인 단일 loss 최소화보다 optimization dynamics가 복잡하다.
- Parzen window likelihood는 bandwidth와 sample 수에 민감하고 시각 품질과 일치하지 않을 수 있다.
- nearest-neighbor 비교는 직접 복사를 반박하는 일부 증거일 뿐, 일반화와 memorization을 완전히 판정하지는 못한다.
- 이 논문은 오늘날의 DCGAN, WGAN, normalization, 안정화 기법 이전의 초기 프레임워크다. 후속 연구의 개선 사항을 원 논문의 결과로 소급해 해석하면 안 된다.

## 9. 기존 연구 대비 차별점

| 방법 | Explicit density | 학습 중 inference | MCMC sampling | 생성 방식 |
|---|---:|---:|---:|---|
| Directed latent model | 있음 | 필요할 수 있음 | 불필요 | ancestral/decoder |
| RBM/DBM | 비정규화 density | 상황에 따라 필요 | 필요 | Markov chain |
| GSN/denoising 계열 | 보통 없음 | 학습 transition에 따라 다름 | 필요 | learned chain |
| VAE | likelihood/ELBO | encoder 필요 | 불필요 | decoder forward |
| **GAN** | 없음 | 없음 | 없음 | generator forward |

핵심 차별점은 고정 noise와 데이터를 구분하는 것이 아니라, **학습하며 변하는 generator와 discriminator를 하나의 game으로 결합**했다는 점이다.

## 10. 핵심 아이디어를 직관적으로 이해하기

판별자는 두 분포가 어디에서 다른지 찾아내는 자동화된 loss function 역할을 한다. 사람이 “좋은 이미지”를 픽셀 단위 수식으로 정의하는 대신, 판별자가 실제 데이터와 다른 특징을 학습하고 그 gradient를 생성자에게 전달한다.

완벽한 균형에서는 생성 분포가 실제 분포와 같아 판별자가 동전을 던지는 것보다 잘할 수 없다. 그러나 실전 학습의 핵심은 이 최종 상태 자체보다, 그 상태로 향하는 동안 두 모델의 학습 속도와 gradient 품질을 안정적으로 유지하는 데 있다.

## 11. 결론 및 주요 요약 (Conclusion)

이 논문은 adversarial training이 실제 생성 모델 학습에 가능함을 처음 체계적으로 보였다. Generator와 discriminator의 minimax game, 최적 discriminator, Jensen-Shannon divergence와의 연결, non-saturating generator loss를 제시했다. 실험은 작고 평가도 초기 단계였지만, explicit likelihood·approximate inference·MCMC 없이 신경망과 backpropagation만으로 복잡한 데이터를 생성할 수 있다는 새로운 연구 방향을 열었다.

논문이 제안한 후속 방향에는 conditional generation, `x -> z` inference network, semi-supervised learning, 공유 파라미터를 이용한 여러 conditional distribution 학습, `G/D` 조정 방법 개선이 포함된다. 상당수가 이후 실제 GAN 연구의 주요 주제가 되었다.

## 12. 재현 및 공부 포인트

- 먼저 1D 또는 2D Gaussian mixture에서 `p_data`, `p_g`, `D`의 변화를 시각화한다.
- minimax loss와 non-saturating loss의 generator gradient 크기를 비교한다.
- 매 step `D(real)`, `D(fake)`, `loss_D`, `loss_G`를 기록하되 단독 지표로 성공을 판단하지 않는다.
- latent interpolation과 sample diversity를 함께 확인한다.
- discriminator update 횟수 `k`, learning rate, capacity 균형을 ablation한다.
- mode coverage를 측정할 수 있는 합성 데이터로 collapse를 확인한다.
- 원 논문 재현과 현대적 안정화 기법이 들어간 구현을 구분해 기록한다.

## 참고

- 원문: `1406.2661.pdf`
- 원 논문 코드: <http://www.github.com/goodfeli/adversarial>
- 주요 원문 위치: Introduction pp.1-2, Related Work p.2, Method pp.2-4, Theory pp.3-5, Experiments pp.5-7, Advantages/Disadvantages p.7, Conclusion pp.7-8
