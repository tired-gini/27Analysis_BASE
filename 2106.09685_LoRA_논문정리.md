# LoRA: Low-Rank Adaptation of Large Language Models

> Edward J. Hu et al., arXiv:2106.09685v2 (2021)  
> 한 줄 요약: 사전학습 가중치는 고정하고, 태스크 적응에 필요한 **가중치 변화량만 저랭크 행렬로 학습**해 전체 미세조정 수준의 성능을 훨씬 적은 비용으로 달성한다.

## 0. 핵심 요약

LoRA(Low-Rank Adaptation)의 출발점은 “거대한 사전학습 모델을 태스크마다 전부 복제하고 미세조정하는 것은 비싸다”는 문제다. 저자들은 미세조정 중 생기는 가중치 변화량 `ΔW`가 낮은 내재 랭크(intrinsic rank)를 가질 것이라고 가정한다. 이에 원래 가중치 `W₀`는 고정하고 `ΔW = BA`만 학습한다. `A ∈ R^(r×k)`, `B ∈ R^(d×r)`이며 `r << min(d, k)`이므로 학습 파라미터 수가 크게 감소한다.

GPT-3 175B에서는 전체 미세조정 대비 학습 파라미터를 최대 약 10,000배 줄이고, Adam 학습 시 GPU 메모리를 약 1/3 수준으로 낮추면서도 WikiSQL, MNLI, SAMSum에서 전체 미세조정과 동등하거나 더 좋은 결과를 보였다. 추론 전 `BA`를 `W₀`에 병합할 수 있어 adapter와 달리 추가 추론 지연이 없다.

## 1. 어떤 문제를 해결하려 했는가? (Abstract)

사전학습-미세조정 패러다임에서 모델이 커질수록 다음 문제가 심각해진다.

- 태스크마다 원본과 같은 크기의 전체 모델 체크포인트가 필요하다.
- Adam은 파라미터뿐 아니라 gradient와 optimizer state까지 저장하므로 학습 메모리가 매우 크다.
- 여러 사용자나 태스크를 서비스할 때 모델 복제와 태스크 전환 비용이 크다.
- 기존 parameter-efficient 방법은 성능, 입력 길이, 또는 추론 지연 중 일부를 희생한다.

LoRA는 사전학습 가중치를 동결하고 Transformer의 선형층에 작은 저랭크 경로를 병렬로 추가한다. 목표는 다음 네 가지를 동시에 만족하는 것이다.

1. 학습 파라미터와 optimizer state 축소
2. 태스크별 체크포인트 축소
3. 전체 미세조정에 준하는 품질
4. 배포 시 추가 추론 지연 제거

## 2. 연구 동기와 기존 문제점 (Introduction)

### 2.1 전체 미세조정의 배포 문제

GPT-3 175B 같은 모델을 태스크마다 전체 미세조정하면 각 태스크에 약 175B개의 파라미터 변화량이 생긴다. 논문 예시에서 하나의 전체 체크포인트는 약 350GB(FP16 기준)이며, 태스크 100개를 별도 저장하면 약 35TB가 필요하다.

### 2.2 기존 효율적 적응법의 trade-off

- **Adapter**: Transformer 블록 사이에 작은 신경망을 직렬로 삽입한다. 파라미터는 적지만 네트워크 깊이와 연산 단계가 늘어난다. GPT-2 medium, batch size 1, sequence length 128 실험에서 Adapter는 기본 모델보다 약 20.7~30.3% 느렸다(Table 1).
- **Prefix/prompt tuning**: 입력 또는 각 층의 activation에 학습 가능한 virtual token을 둔다. 최적화가 불안정할 수 있고, 학습 가능한 token이 실제 입력에 사용할 문맥 길이를 차지한다.
- **일부 층만 미세조정**: 전체 미세조정보다 작지만 여전히 큰 변화량을 저장하며 어떤 층을 고를지에 민감하다.

저자들은 과매개변수화 모델의 해가 낮은 intrinsic dimension에 존재한다는 선행 연구에서 착안해, “모델 자체가 아니라 **태스크 적응에 필요한 업데이트**가 저랭크일 수 있다”고 가정했다.

## 3. 관련 연구 동향 (Related Works)

### 3.1 사전학습과 전체 미세조정

BERT와 GPT 계열은 대규모 일반 말뭉치에서 사전학습한 뒤 태스크별 전체 파라미터를 업데이트하는 방식을 정착시켰다. 성능은 강하지만 모델 크기 증가와 함께 저장·학습 비용도 선형으로 커진다.

### 3.2 Parameter-efficient adaptation

- Adapter 계열: 병목 MLP를 블록 내부에 삽입한다.
- Prefix/Prompt 계열: 연속 벡터 형태의 prompt 또는 층별 prefix activation만 학습한다.
- BitFit: bias만 학습한다.
- Partial fine-tuning: 마지막 일부 층 등 제한된 파라미터만 학습한다.

LoRA의 차이는 새로운 비선형 모듈을 직렬로 추가하지 않고, 기존 선형 가중치의 **업데이트를 저랭크로 재매개변수화**한다는 점이다.

### 3.3 저랭크 구조

행렬 완성, 저랭크 근사, 신경망 압축에서는 저랭크 구조가 오래 연구되었다. 그러나 기존 연구는 주로 원래 가중치 자체를 저랭크로 제한하거나 압축했다. LoRA는 사전학습 모델을 고정한 채 **다운스트림 업데이트 `ΔW`**에 저랭크 제약을 둔다는 점이 다르다.

## 4. 연구 접근법과 모델 구조 (Method)

### 4.1 문제 정의

사전학습 autoregressive 모델을 `P_Φ(y|x)`라 하자. 전체 미세조정은 `Φ₀`에서 시작해 모든 파라미터 변화량 `ΔΦ`를 학습한다.

$$
\max_{\Phi}\sum_{(x,y)\in Z}\sum_{t=1}^{|y|}
\log P_{\Phi}(y_t\mid x,y_{<t})
$$

LoRA는 태스크별 변화량을 훨씬 작은 파라미터 집합 `Θ`로 표현한다.

$$
\Delta\Phi=\Delta\Phi(\Theta),\qquad |\Theta|\ll |\Phi_0|
$$

따라서 최적화 대상은 원본 모델 전체가 아니라 `Θ`가 된다.

### 4.2 저랭크 업데이트

원래 선형층의 가중치가 `W₀ ∈ R^(d×k)`일 때 다음과 같이 쓴다.

$$
W = W_0 + \Delta W = W_0 + BA
$$

$$
B\in\mathbb{R}^{d\times r},\quad
A\in\mathbb{R}^{r\times k},\quad
r\ll\min(d,k)
$$

입력 `x`에 대한 forward pass는 다음과 같다.

$$
h=W_0x+\frac{\alpha}{r}BAx
$$

- `W₀`: 동결, gradient 없음
- `A`: Gaussian으로 초기화
- `B`: 0으로 초기화해 학습 시작 시 `ΔW = 0`
- `α/r`: 업데이트 크기 조절. 논문은 첫 시도의 `r`에 `α`를 맞추고 별도 튜닝하지 않는 방식을 사용한다.

학습 파라미터 수는 원래 `dk`에서 LoRA의 `r(d+k)`로 감소한다. 정사각 가중치 `d×d`라면 `d²` 대신 `2dr`이다.

### 4.3 Transformer에 적용

Self-attention에는 `W_q`, `W_k`, `W_v`, `W_o`가 있고 MLP에도 두 개의 큰 선형층이 있다. 논문은 단순성과 효율성을 위해 주로 모든 Transformer 층의 `W_q`, `W_v`에만 LoRA를 적용하고 MLP는 동결했다.

같은 파라미터 예산에서 한 종류의 행렬에 큰 rank를 몰아주는 것보다 여러 attention projection에 작은 rank를 분산하는 편이 좋았다. GPT-3 분석에서 `W_q`와 `W_v`를 함께 적응하는 구성이 전반적으로 우수했다(Table 5).

### 4.4 학습과 추론

학습 중에는 원래 경로와 LoRA 경로를 병렬 계산한다. 배포 전에는 다음처럼 병합할 수 있다.

$$
W_{\text{merged}} = W_0 + \frac{\alpha}{r}BA
$$

병합 후 forward pass는 원래 선형층과 같아 추가 latency가 없다. 태스크를 바꾸려면 기존 `BA`를 빼고 다른 태스크의 `B'A'`를 더한다.

### 4.5 개념 코드 분석

아래 코드는 논문의 식을 PyTorch 형태로 옮긴 최소 구현이다. 논문 공식 저장소 코드의 전재가 아니라 동작 원리를 설명하기 위한 구현이다.

```python
import math
import torch
from torch import nn
from torch.nn import functional as F


class LoRALinear(nn.Module):
    def __init__(self, base: nn.Linear, rank: int = 8, alpha: int = 8):
        super().__init__()
        self.base = base
        self.rank = rank
        self.scale = alpha / rank

        # A: (r, in), B: (out, r)
        self.lora_A = nn.Parameter(torch.empty(rank, base.in_features))
        self.lora_B = nn.Parameter(torch.zeros(base.out_features, rank))
        nn.init.kaiming_uniform_(self.lora_A, a=math.sqrt(5))

        # W_0와 bias는 학습하지 않는다.
        self.base.weight.requires_grad_(False)
        if self.base.bias is not None:
            self.base.bias.requires_grad_(False)

    def forward(self, x):
        frozen_path = self.base(x)                  # W_0 x
        low_rank_path = F.linear(F.linear(x, self.lora_A), self.lora_B)
        return frozen_path + self.scale * low_rank_path

    @torch.no_grad()
    def merged_weight(self):
        return self.base.weight + self.scale * (self.lora_B @ self.lora_A)
```

코드에서 중요한 점은 다음과 같다.

- `B=0`이므로 최초 출력은 원본 모델과 완전히 같다.
- `A`와 `B`의 곱만 `W₀`와 같은 shape을 갖는다.
- optimizer에는 `requires_grad=True`인 LoRA 파라미터만 전달해야 메모리 절감 효과가 난다.
- 병합은 선형이므로 가능하다. dropout이나 비선형을 `BA` 사이에 넣으면 같은 방식으로 완전히 병합할 수 없다.

## 5. 실험 구성 (Experiments)

### 5.1 모델과 태스크

| 모델 | 규모 | 태스크/데이터셋 | 목적 |
|---|---:|---|---|
| RoBERTa base/large | 125M / 355M | GLUE | NLU 성능 비교 |
| DeBERTa XXL | 1.5B | GLUE | 더 큰 encoder 모델 검증 |
| GPT-2 medium/large | 355M / 774M | E2E, DART, WebNLG | NLG 성능 비교 |
| GPT-3 | 175B | WikiSQL, MNLI, SAMSum | 초거대 모델 확장성 검증 |

비교군은 full fine-tuning, 일부 층 미세조정, BitFit, prefix-embedding, prefix-layer tuning, 여러 adapter 변형이다. 모든 실험은 NVIDIA Tesla V100을 사용했다.

### 5.2 주요 결과

#### GLUE

- RoBERTa base: LoRA 0.3M 학습 파라미터로 평균 87.2, 전체 미세조정 125M의 86.4보다 높았다.
- RoBERTa large: LoRA 0.8M으로 평균 89.0, 전체 미세조정 355M의 88.9와 사실상 동등했다.
- DeBERTa XXL: LoRA 4.7M으로 평균 91.3, 전체 미세조정 1.5B의 91.1과 동등 이상이었다.

#### GPT-2 생성

E2E NLG에서 GPT-2 medium LoRA는 0.35M 파라미터로 BLEU 70.4를 기록해 전체 미세조정의 68.2를 넘어섰다. DART와 WebNLG에서도 비슷한 예산의 prefix/adapters와 동등하거나 더 좋았다.

#### GPT-3 175B

| 방법 | 학습 파라미터 | WikiSQL | MNLI-m | SAMSum R1/R2/RL |
|---|---:|---:|---:|---:|
| Full FT | 175,255.8M | 73.8 | 89.5 | 52.0/28.0/44.5 |
| AdapterH | 40.1M | 73.2 | 91.5 | 53.2/29.0/45.1 |
| LoRA | 4.7M | 73.4 | **91.7** | **53.8/29.8/45.9** |
| LoRA | 37.7M | **74.0** | 91.6 | 53.4/29.2/45.1 |

파라미터를 더 늘린다고 항상 성능이 오르지는 않았다. Prefix 계열은 token 수가 지나치게 커지면 성능이 하락했고, LoRA는 작은 rank부터 성능이 빠르게 포화했다.

### 5.3 효율성

- GPT-3 175B에서 Adam 학습 메모리: 약 1.2TB → 350GB
- `r=4`, `W_q/W_v` 적용 시 태스크 체크포인트: 약 350GB → 35MB
- 같은 weight sharding 조건에서 처리량: V100당 32.5 → 43.1 token/s, 약 25% 향상
- 배포 시 LoRA 가중치 병합 가능: 추가 추론 지연 없음

### 5.4 Ablation과 분석

#### 어느 가중치에 적용할 것인가?

GPT-3에서 같은 18M 파라미터 예산이라면 `W_q` 또는 `W_k` 하나만 적응하는 것보다 `W_q + W_v`, 혹은 네 attention projection에 더 작은 rank를 분산하는 편이 좋았다.

#### rank는 얼마나 커야 하는가?

- GPT-3 WikiSQL/MNLI에서는 `W_q + W_v`에 rank 1만 적용해도 경쟁력 있는 성능을 냈다.
- GPT-2 E2E에서는 validation loss가 `r=16`, BLEU가 `r=4` 부근에서 가장 좋았다.
- 따라서 “항상 rank 1이면 충분하다”가 아니라, 필요한 rank가 모델·태스크에 따라 달라진다는 결론이 적절하다.

#### 업데이트는 무엇을 학습하는가?

서로 다른 rank와 random seed에서 얻은 `ΔW`의 singular subspace를 비교한 결과, 상위 singular direction은 반복해서 나타났지만 나머지는 겹침이 적었다. 이는 유용한 업데이트 방향이 실제로 저차원일 가능성을 뒷받침한다.

또 `ΔW`는 `W`와 완전히 무관한 무작위 방향은 아니지만, `W`의 최상위 singular direction을 그대로 반복하지도 않았다. 저자들은 이를 사전학습 과정에서 이미 존재하지만 강조되지 않았던 태스크 특화 feature를 `ΔW`가 증폭하는 것으로 해석한다. `r=4` 분석에서는 증폭 계수가 약 20이었다.

## 6. 무엇을 발견했고 한계점은 무엇인가? (Discussion)

### 6.1 발견

1. 거대 언어모델의 다운스트림 적응 업데이트는 매우 낮은 rank로도 표현될 수 있다.
2. rank를 크게 하는 것보다 적절한 여러 weight matrix에 작은 rank를 배치하는 것이 중요할 수 있다.
3. 저랭크 제약은 효율만 높이는 것이 아니라 일부 실험에서 regularization처럼 작용해 전체 미세조정보다 좋은 성능을 냈다.
4. LoRA 업데이트는 사전학습 가중치의 지배적 방향을 복제하기보다 약하게 표현된 태스크 관련 방향을 증폭하는 것으로 보인다.

### 6.2 논문이 인정한 한계

- LoRA를 원본 weight에 병합해 latency를 없앤 상태에서는 한 batch 안에서 샘플마다 서로 다른 LoRA 태스크를 적용하기 어렵다.
- 실험은 주로 attention의 `W_q`, `W_v`에 집중했고 MLP, LayerNorm, bias의 체계적 비교는 남아 있다.
- 어떤 행렬에 LoRA를 적용할지 heuristic에 의존한다.
- 작은 rank가 모든 태스크에 충분하다고 보장할 수 없다. 사전학습과 매우 다른 언어나 도메인에서는 더 큰 rank 또는 전체 미세조정이 유리할 수 있다.
- 이론적으로 `ΔW`가 왜 저랭크가 되는지 완전히 설명하지 못하고 경험적 증거를 제시하는 수준이다.

### 6.3 비판적으로 볼 점

- 여러 표의 baseline 수치는 선행 연구에서 가져온 것도 있어 학습 recipe가 완전히 동일한 일대일 비교는 아니다. 저자들은 일부 공정 비교 설정을 별도로 표시했다.
- 검증셋 최고 epoch 선택과 hyperparameter sweep이 방법마다 다르면 비교에 selection bias가 생길 수 있다.
- GPT-3 175B는 비용 때문에 각 항목의 seed별 분산을 모두 보고하지 않았다.
- “추론 latency 0”은 **병합했을 때**의 주장이다. 여러 adapter를 동적으로 조합하거나 샘플별 adapter routing을 하면 병합의 장점이 약해진다.

## 7. 기존 연구 대비 차별점

| 방법 | 학습 대상 | 입력 길이 감소 | 추가 추론 지연 | 태스크별 저장량 |
|---|---|---:|---:|---:|
| Full fine-tuning | 원본 전체 파라미터 | 없음 | 없음 | 매우 큼 |
| Adapter | 직렬 bottleneck module | 없음 | 있음 | 작음 |
| Prefix tuning | virtual token/activation | 있을 수 있음 | 있음 | 작음 |
| BitFit | bias | 없음 | 없음 | 매우 작음 |
| **LoRA** | 병렬 저랭크 `ΔW=BA` | 없음 | 병합 시 없음 | 매우 작음 |

LoRA의 핵심 차별점은 **가중치 업데이트 공간을 제한하는 재매개변수화**와 **선형성 덕분의 완전 병합**이다.

## 8. 핵심 아이디어를 직관적으로 이해하기

전체 미세조정은 `d×k` 공간의 모든 방향으로 가중치를 움직일 수 있게 한다. LoRA는 실제 태스크가 필요로 하는 변화가 몇 개의 중요한 방향 조합이라면 전체 공간을 열 필요가 없다고 본다. `A`가 입력 feature를 `r`차원으로 압축하고, `B`가 그 `r`개 방향을 출력 공간으로 다시 펼친다고 볼 수 있다.

즉 LoRA는 “모델이 몰랐던 것을 새로 대규모로 학습한다”기보다 “이미 배운 표현 중 태스크에 필요한 방향을 선택하고 증폭한다”는 관점과 잘 맞는다.

## 9. 재현 및 공부 포인트

- 첫 구현은 모든 층의 `q_proj`, `v_proj`에 `r=4` 또는 `r=8`을 적용해 보는 것이 좋다.
- trainable parameter 목록을 출력해 base weight가 실제로 freeze되었는지 확인한다.
- LoRA checkpoint에는 `A`, `B`와 필요한 설정(`rank`, `alpha`, target modules)을 함께 저장한다.
- merge 전후 출력이 수치 오차 범위에서 같은지 테스트한다.
- 실험 비교 시 전체 학습 파라미터 수뿐 아니라 peak VRAM, 학습 처리량, 체크포인트 크기, inference latency를 함께 측정한다.
- rank, target module, learning rate를 독립적으로 ablation해야 한다.

## 10. 결론

LoRA는 거대 모델 적응의 비용 문제를 “전체 가중치를 바꾸지 말고, 변화량을 저랭크로 표현하자”는 단순한 아이디어로 해결한다. 파라미터·메모리·저장 공간을 크게 줄이면서 여러 NLU/NLG 실험에서 전체 미세조정과 동등하거나 더 좋은 성능을 보였고, 병합 가능한 구조로 추론 latency도 추가하지 않는다. 오늘날 PEFT의 대표 방식이 된 이유는 이론적 가정이 단순하고, 코드 변경이 작으며, 학습과 배포 양쪽에서 실용적이기 때문이다.

## 참고

- 원문: `2106.09685v2.pdf`
- 논문 공식 구현: <https://github.com/microsoft/LoRA>
- 주요 원문 위치: Method pp.4-5, Experiments pp.5-8, Related Works pp.8-9, low-rank 분석 pp.9-12, Conclusion pp.12-13, 추가 실험 pp.17-26
