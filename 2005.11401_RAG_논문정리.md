# Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks

> Patrick Lewis et al., arXiv:2005.11401v4 (NeurIPS 2020)  
> 한 줄 요약: 사전학습 seq2seq 모델의 parametric memory와 Wikipedia 검색기의 non-parametric memory를 결합하고, 검색 문서를 잠재변수로 주변화해 검색기와 생성기를 함께 미세조정한다.

## 0. 핵심 요약

대규모 언어모델은 사실 지식을 파라미터에 저장하지만, 지식을 정확히 꺼내거나 최신 정보로 수정하거나 답의 근거를 보여주기 어렵다. RAG(Retrieval-Augmented Generation)는 질문 `x`로 관련 문서 `z`를 검색하고, 원 입력과 문서를 함께 generator에 넣어 출력 `y`를 생성한다.

이 논문은 DPR retriever와 BART-large generator를 결합하고 검색 문서를 관측되지 않은 잠재변수로 취급한다. 하나의 문서가 전체 출력 시퀀스를 설명하는 **RAG-Sequence**와, token마다 다른 문서의 기여를 섞는 **RAG-Token**을 제안한다. 별도의 정답 문서 supervision 없이 query encoder와 generator를 end-to-end로 학습하며, open-domain QA 세 과제에서 당시 SOTA를 달성하고 BART보다 더 구체적·사실적·다양한 텍스트를 생성했다.

## 1. 어떤 문제를 해결하려 했는가? (Abstract)

Parametric-only 언어모델의 문제는 다음과 같다.

- 파라미터 속 지식을 정확하게 접근하고 조작하기 어렵다.
- 세계 지식이 바뀔 때 재학습 없이 수정하기 어렵다.
- 답변이 어떤 자료에 근거했는지 provenance를 제시하기 어렵다.
- knowledge-intensive task에서는 전용 검색·추출 시스템보다 성능이 낮을 수 있다.
- 사실처럼 보이지만 틀린 hallucination을 생성할 수 있다.

기존의 differentiable retriever 결합 연구는 주로 extractive QA에 집중했다. RAG는 이를 범용 seq2seq 생성으로 확장해, 검색 결과를 그대로 답으로 추출하는 대신 검색 문서를 조건으로 자유로운 텍스트를 생성한다.

## 2. 연구 동기와 문제점 (Introduction)

### 2.1 두 종류의 기억

- **Parametric memory**: BART의 가중치에 내재된 언어 능력과 지식. 부드러운 생성·추론에는 강하지만 출처 확인과 갱신이 어렵다.
- **Non-parametric memory**: Wikipedia passage의 dense vector index. 사람이 읽고 수정할 수 있으며 검색 결과를 근거로 확인할 수 있다.

RAG의 핵심 질문은 “두 기억을 하나의 확률 모델에서 어떻게 결합해 다양한 생성·분류 태스크에 적용할 것인가?”이다.

### 2.2 기존 방법의 한계

- Closed-book QA는 모델 파라미터에만 의존하므로 큰 모델이 필요하고 최신성·근거성이 약하다.
- Retrieve-and-extract QA는 검색 문서에 정답 문자열이 반드시 있어야 한다. 여러 문서의 단서를 종합하거나 자유 형식 답을 만들기 어렵다.
- REALM, ORQA 등은 neural retrieval의 가능성을 보였지만 주로 extractive QA 또는 특화된 사전학습에 초점을 맞췄다.
- 태스크별 retrieval architecture는 강하지만 범용 seq2seq 모델처럼 여러 출력 형식으로 재사용하기 어렵다.

## 3. 관련 연구 동향 (Related Works)

### 3.1 Single-task retrieval

검색은 open-domain QA, fact checking, dialogue, translation, language modeling 등 각 태스크에서 개별적으로 사용되어 왔다. RAG는 하나의 retrieval-augmented architecture를 여러 지식 집약적 태스크에 미세조정할 수 있음을 보인다.

### 3.2 범용 사전학습 모델

GPT-2, BART, T5는 하나의 사전학습 모델을 분류와 생성에 폭넓게 적용했지만 외부 검색 없이 parametric memory에 의존했다. RAG는 범용 seq2seq의 적용 범위에 학습 가능한 검색 모듈을 더한다.

### 3.3 Learned retrieval

DPR 같은 dense bi-encoder는 질문과 문서를 각각 임베딩하고 Maximum Inner Product Search(MIPS)로 검색한다. 기존 연구에는 supervised retrieval, reinforcement learning, latent-variable 학습이 있었다. RAG는 문서 label 없이 최종 출력 likelihood로 query encoder를 학습한다.

### 3.4 Memory network와 retrieve-and-edit

RAG의 Wikipedia index는 큰 외부 memory로 볼 수 있다. 분산된 내부 상태만 저장하는 방식과 달리 raw text를 memory로 사용하므로 사람이 읽고 고칠 수 있다. Retrieve-and-edit가 유사한 학습 예시를 편집하는 데 초점을 둔다면 RAG는 근거 문서를 검색해 여러 문서의 정보를 생성 과정에 결합한다.

## 4. 연구 접근법과 모델 구조 (Method)

### 4.1 전체 구조

입력 `x`가 주어지면 다음 순서로 동작한다.

1. Query encoder가 `x`를 dense vector `q(x)`로 변환한다.
2. Wikipedia passage index에서 내적 점수가 높은 top-k 문서 `z`를 MIPS로 검색한다.
3. 각 문서와 원 입력을 이어 붙여 BART generator에 넣는다.
4. 검색 문서를 잠재변수로 두고 문서별 생성확률을 합산한다.
5. 정답 출력의 negative marginal log-likelihood로 query encoder와 generator를 공동 학습한다.

```text
x ──> DPR query encoder ──> top-k Wikipedia passages z
│                                  │
└──────────────────────────────────┴──> BART generator ──> y
                    pη(z|x)              pθ(y|x,z)
```

### 4.2 Retriever: DPR

DPR은 query와 document를 서로 다른 BERT encoder로 표현하는 bi-encoder다.

$$
p_\eta(z\mid x) \propto
\exp\left(d(z)^\top q(x)\right)
$$

$$
d(z)=\mathrm{BERT}_d(z),\qquad
q(x)=\mathrm{BERT}_q(x)
$$

논문은 DPR의 사전학습된 encoder와 index로 초기화한다. 문서 encoder와 index는 고정하고 query encoder만 generator와 함께 업데이트한다. 문서 encoder까지 바꾸면 전체 passage embedding을 반복 계산해야 하기 때문이다.

### 4.3 Generator: BART-large

Generator는 400M 규모의 BART-large다. 입력 `x`와 각 검색 passage `z`를 concatenate해 encoder에 넣고 autoregressive하게 token을 생성한다.

$$
p_\theta(y_i\mid x,z,y_{<i})
$$

RAG의 특징은 generator가 하나의 정답 span을 복사하도록 제한되지 않는다는 것이다. 검색 문서에 정답이 정확히 나타나지 않아도 여러 단서와 parametric knowledge를 활용해 답을 만들 수 있다.

### 4.4 RAG-Sequence

하나의 문서 `z`가 전체 출력 시퀀스 `y`를 설명한다고 가정한다.

$$
p_{\text{RAG-Seq}}(y\mid x)
\approx
\sum_{z\in \operatorname{top-k}(p_\eta(\cdot\mid x))}
p_\eta(z\mid x)
\prod_{i=1}^{N}p_\theta(y_i\mid x,z,y_{<i})
$$

문서 단위 mixture다. 각 문서 조건에서 전체 문장의 확률을 계산한 뒤 retriever 확률로 가중합한다.

### 4.5 RAG-Token

각 token을 만들 때 서로 다른 문서가 기여할 수 있다고 가정한다.

$$
p_{\text{RAG-Token}}(y\mid x)
\approx
\prod_{i=1}^{N}
\sum_{z\in \operatorname{top-k}(p_\eta(\cdot\mid x))}
p_\eta(z\mid x)
p_\theta(y_i\mid x,z,y_{<i})
$$

합과 곱의 순서가 핵심 차이다.

- RAG-Sequence: `Σ_z Π_i` — 한 문서가 전체 시퀀스를 책임진다.
- RAG-Token: `Π_i Σ_z` — token마다 문서 기여를 다시 섞는다.

출력 길이가 1인 분류에서는 두 모델이 동일하다.

### 4.6 학습 목적함수

정답 문서 label 없이 입력-출력 쌍 `(x_j, y_j)`만 사용한다.

$$
\mathcal{L}=-\sum_j \log p(y_j\mid x_j)
$$

이 gradient는 generator뿐 아니라 `pη(z|x)`에도 전달된다. 정답 생성에 도움을 주는 문서의 검색 확률을 높이는 방식으로 query encoder가 태스크에 맞게 학습된다.

### 4.7 Decoding

- **RAG-Token**: token별로 주변화된 next-token 확률을 표준 beam search에 넣을 수 있다.
- **RAG-Sequence**: 문서별 beam search 후 후보 시퀀스를 다시 모든 관련 문서에 대해 평가해야 정확한 marginal score를 구할 수 있다.
  - Thorough decoding: 누락된 문서-후보 조합도 추가 forward pass로 평가한다.
  - Fast decoding: 특정 문서 beam에 없던 후보의 확률을 0으로 근사해 계산을 줄인다.

### 4.8 개념 코드 분석

아래는 두 주변화 방식을 log-space로 표현한 의사 구현이다. 실제 RAG에서는 token shift, padding mask, beam search 등의 처리가 추가된다.

```python
import torch


def rag_sequence_log_prob(retriever_logits, token_log_probs):
    """
    retriever_logits: [batch, k]
    token_log_probs:  [batch, k, target_length]
    """
    log_p_z = retriever_logits.log_softmax(dim=-1)       # log p_eta(z|x)
    log_p_y_given_z = token_log_probs.sum(dim=-1)        # Σ_i log p_theta(y_i|x,z,y_<i)
    return torch.logsumexp(log_p_z + log_p_y_given_z, dim=-1)


def rag_token_log_prob(retriever_logits, token_log_probs):
    log_p_z = retriever_logits.log_softmax(dim=-1)       # [B, K]
    token_mixture = torch.logsumexp(
        log_p_z.unsqueeze(-1) + token_log_probs, dim=1   # 문서 K개를 token마다 주변화
    )
    return token_mixture.sum(dim=-1)                     # token log-prob의 합
```

`logsumexp`를 쓰는 이유는 작은 확률을 직접 곱하고 더할 때 생기는 underflow를 피하기 위해서다. 두 함수에서 `sum`과 `logsumexp`의 순서가 논문의 두 모델 차이를 그대로 나타낸다.

## 5. 실험 구성 (Experiments)

### 5.1 공통 검색 corpus

- 2018년 12월 Wikipedia dump
- 문서를 서로 겹치지 않는 100-word passage로 분할
- 총 약 21M passage
- DPR document encoder로 embedding 생성
- FAISS HNSW index로 MIPS 수행
- 학습 시 top-k는 5 또는 10, 테스트 시 dev set으로 결정

### 5.2 태스크와 지표

| 태스크 | 데이터셋 | 출력/지표 |
|---|---|---|
| Open-domain QA | NQ, TriviaQA, WebQuestions, CuratedTREC | Exact Match |
| Abstractive QA | MS MARCO NLG | BLEU-1, ROUGE-L |
| Question generation | SearchQA 기반 Jeopardy | BLEU-1, Q-BLEU-1, 인간평가 |
| Fact verification | FEVER 3-way/2-way | Label accuracy |

학습은 mixed precision, 8개의 32GB V100 GPU로 수행했다. Wikipedia index vector는 CPU에 저장했으며 최초 구성은 약 100GB, 후속 압축 구현은 약 36GB가 필요했다. Appendix의 구성요소 합계는 DPR query/document encoder 각 110M과 BART 406M으로 626M이지만, document encoder는 고정하므로 실제 업데이트 대상은 query encoder와 BART다.

## 6. 실험 결과

### 6.1 Open-domain QA

| 모델 | NQ | TriviaQA | WebQuestions | CuratedTREC |
|---|---:|---:|---:|---:|
| DPR | 41.5 | 57.9 | 41.1 | 50.6 |
| RAG-Token | 44.1 | 55.2 | **45.5** | 50.0 |
| RAG-Sequence | **44.5** | **56.8** | 45.2 | **52.2** |

TriviaQA의 별도 Wiki test split에서는 RAG-Sequence가 68.0을 기록했다. 논문은 NQ, WebQuestions, CuratedTREC에서 당시 SOTA를 달성했다고 보고한다.

특히 NQ에서 top retrieved document들에 정답 문자열이 전혀 없어도 11.8%를 맞혔다. Extractive system이라면 구조적으로 0%인 경우이며, 생성과 parametric memory의 보완 효과를 보여준다.

### 6.2 Abstractive QA와 Jeopardy 생성

- MS MARCO에서 RAG-Sequence는 BART보다 BLEU-1과 ROUGE-L이 각각 2.6점 높았다.
- Jeopardy에서 RAG-Token의 Q-BLEU-1은 22.2, RAG-Sequence는 21.4, BART는 19.7이었다.
- 452개 비교 인간평가에서 RAG가 BART보다 더 factual하다는 선택은 42.7%, 반대는 7.1%였다.
- 더 specific하다는 선택도 RAG 37.4%, BART 16.8%였다.

Jeopardy처럼 한 출력에 여러 사실이 들어가면 token마다 다른 문서를 참조할 수 있는 RAG-Token이 유리할 수 있다. 논문의 Hemingway 예에서는 “The Sun Also Rises”와 “A Farewell to Arms”를 생성할 때 서로 다른 문서 posterior가 커졌다.

### 6.3 Fact verification

- FEVER 3-way: RAG 72.5, 당시 SOTA 76.8
- FEVER 2-way: RAG 89.5, gold evidence를 쓰는 비교 모델 92.2
- RAG는 별도 evidence supervision 없이 claim만 보고 스스로 문서를 검색했다.
- top-1 검색 문서가 gold article인 비율은 71%, top-10 안에 gold article이 있는 비율은 90%였다.

### 6.4 Diversity

Distinct trigram 비율은 다음과 같았다.

| 모델 | MS MARCO | Jeopardy |
|---|---:|---:|
| BART | 70.7% | 32.4% |
| RAG-Token | 77.8% | 46.8% |
| RAG-Sequence | 83.5% | 53.8% |

별도의 diversity decoding 없이도 검색 문서가 생성의 조건을 다양화했다.

### 6.5 Retrieval ablation

- 학습 중 retriever를 고정하면 대부분 태스크에서 성능이 하락했다. 최종 출력 supervision이 query encoder를 태스크에 맞게 개선한다는 증거다.
- BM25는 lexical overlap이 강한 FEVER에서 잘했지만, open-domain QA 등 대부분 태스크에서는 learned dense retrieval보다 낮았다.
- test 시 검색 문서 수를 늘리면 RAG-Sequence의 NQ 성능은 단조 증가했지만 RAG-Token은 약 10개에서 정점을 보였다. 문서를 많이 검색한다고 항상 좋은 것은 아니다.

### 6.6 지식 갱신: index hot-swapping

2016년과 2018년 Wikipedia index를 교체해 당시 세계 지도자를 묻는 실험을 했다.

- 2016 index + 2016 정답: 70%
- 2018 index + 2018 정답: 68%
- 2018 index + 2016 정답: 12%
- 2016 index + 2018 정답: 4%

모델 재학습 없이 index만 교체해 답변의 시점을 바꿀 수 있다는 것을 보여준다.

## 7. 무엇을 발견했고 한계점은 무엇인가? (Discussion)

### 7.1 발견

1. Parametric memory와 검색 기반 non-parametric memory는 서로 대체 관계가 아니라 상호 보완적이다.
2. 검색 문서를 latent variable로 두면 문서 정답 label 없이 end-to-end retriever 학습이 가능하다.
3. 생성형 QA는 검색 문서에 정답 span이 없어도 단서를 합성할 수 있다.
4. 외부 index는 모델 지식을 읽을 수 있고(human-readable), 수정할 수 있는(human-writable) memory 역할을 한다.
5. RAG는 parametric-only BART보다 더 factual하고 specific한 출력을 생성했지만 hallucination을 제거한 것은 아니다.

### 7.2 논문과 부록에서 드러난 한계

- 성능은 검색 corpus의 사실성, 편향, 최신성에 의존한다. Wikipedia의 오류나 편향도 답변에 반영될 수 있다.
- 21M passage index는 큰 CPU memory와 검색 infrastructure를 요구한다.
- 문서 encoder를 동결하므로 corpus representation이 downstream task에 완전히 적응하지는 않는다.
- top-k truncation은 전체 문서 분포에 대한 정확한 주변화가 아니라 근사다. 필요한 문서가 top-k에 없으면 generator가 복구하기 어렵다.
- RAG-Sequence의 thorough decoding은 문서 수와 후보 수가 커지면 forward pass가 많아진다.
- 일부 장문 생성 태스크에서는 retriever가 입력과 무관하게 같은 문서를 찾는 **retrieval collapse**가 관찰되었다. 이때 generator는 검색 문서를 무시하고 BART처럼 동작한다.
- FEVER의 evidence sentence 추출은 다루지 않았고, label accuracy가 곧 완전한 provenance 제공을 뜻하지 않는다.
- Null-document를 여러 방식으로 추가했지만 성능 향상이 없어 제외했다. 검색할 지식이 없는 질문을 명시적으로 판별하지는 못한다.

### 7.3 비판적으로 볼 점

- Open-domain QA에서 retriever가 NQ와 TriviaQA supervision으로 사전학습된 DPR로 초기화되므로 “retrieval supervision이 전혀 없다”기보다 **RAG fine-tuning 단계에 직접 문서 label이 없다**고 해석해야 정확하다.
- 데이터셋마다 test retrieval 문서 수와 decoding 방식이 다르므로 계산비용까지 포함한 공정 비교가 필요하다.
- Factuality 인간평가는 상대 비교이고, 평가자 불일치 또는 외부 검색 품질의 영향을 받을 수 있다.
- 외부 문서가 근거 후보를 제공하지만 생성된 문장의 각 주장과 문서 간 entailment를 보장하지 않는다. 검색 provenance와 답변 충실성(faithfulness)은 별도 문제다.
- index hot-swap은 시간에 따른 갱신 가능성을 보여주지만 82개 지도자 질문의 제한된 실험이다.

## 8. 기존 연구 대비 차별점

| 접근 | 외부 지식 | 출력 방식 | 지식 갱신 | 근거 확인 |
|---|---|---|---|---|
| Parametric-only BART/T5 | 없음 | 자유 생성 | 재학습 필요 | 어려움 |
| Retrieve-and-extract | 있음 | 문서 span 추출 | index 교체 | 비교적 쉬움 |
| REALM/ORQA 계열 | 있음 | 주로 extractive QA | 가능 | 가능 |
| **RAG** | dense text index | 범용 seq2seq 생성 | index 교체 | 검색 passage 확인 가능 |

가장 큰 차별점은 이미 강하게 사전학습된 retriever와 generator를 확률적으로 결합하고, 검색 문서를 latent variable로 주변화해 **추출·생성·분류를 하나의 구조로 처리**한다는 것이다.

## 9. 핵심 아이디어를 직관적으로 이해하기

RAG는 답을 만들기 전에 “어느 자료가 맞는가?”를 하나로 확정하지 않는다. 상위 `k`개 문서를 가져와 각 문서가 답을 얼마나 잘 설명하는지 평가하고, retriever의 신뢰도와 generator의 신뢰도를 함께 사용한다.

$$
\text{최종 신뢰도}
\approx
\sum_{\text{문서}}
(\text{이 문서가 관련 있을 확률})
\times
(\text{이 문서로 답을 만들 확률})
$$

이 soft marginalization 덕분에 초기 검색의 불확실성을 유지하면서 generator가 여러 후보를 활용할 수 있다.

## 10. 재현 및 공부 포인트

- 먼저 small corpus와 FAISS index로 retrieval 결과를 눈으로 검증한다.
- `p(z|x)`와 `p(y|x,z)`를 반드시 log-space에서 결합한다.
- RAG-Sequence와 RAG-Token에서 `sum`과 `product`의 순서가 바뀌는 것을 코드로 직접 확인한다.
- generator 성능만 보지 말고 retrieval recall@k, gold document hit rate, answer faithfulness를 따로 측정한다.
- retriever freeze/joint training, BM25/dense retrieval, `k`, corpus 시점, chunk size를 ablation한다.
- index 갱신 후 모델을 다시 학습하지 않아도 되는지, 새 문서와 기존 parametric knowledge가 충돌할 때 무엇을 답하는지 확인한다.
- 실제 시스템에서는 검색 latency, index memory, 문서 access control, 악성 문서 삽입 및 prompt injection 위험도 함께 고려해야 한다.

## 11. 결론

RAG는 언어모델의 내부 지식과 외부 검색을 하나의 latent-variable 생성 모델로 결합했다. Open-domain QA에서 강한 성능을 내고, BART보다 더 사실적·구체적·다양한 생성을 보였으며, index 교체만으로 지식을 갱신할 수 있음을 보였다. 핵심 공헌은 단순히 “검색 후 생성”이라는 pipeline이 아니라, 검색 문서의 불확실성을 확률적으로 주변화하고 최종 출력 loss로 retriever와 generator를 함께 적응시킨 데 있다.

## 참고

- 원문: `2005.11401v4.pdf`
- 당시 공개 구현: 논문 부록에 안내된 Hugging Face Transformers RAG 예제
- 주요 원문 위치: Method pp.2-4, Experiments pp.4-5, Results pp.5-8, Related Work pp.8-9, Discussion/Broader Impact pp.9-10, 구현 및 추가 한계 pp.17-19
